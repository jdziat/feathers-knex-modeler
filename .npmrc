# Resolve all packages from the public npm registry.
# This open-source package is published to public npm and should build the
# same way for every contributor/CI runner, regardless of any private
# registry configured in the user/global ~/.npmrc.
registry=https://registry.npmjs.org/
